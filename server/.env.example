# Copy to .env — do NOT commit .env. Credentials are backend-only; never in frontend or Git.
# Keys: https://dashboard.plaid.com/developers/keys

PORT=3001
PLAID_CLIENT_ID=
PLAID_SECRET=
# Sandbox (default) or production for real-account pilot. One real account allowed in pilot.
PLAID_ENV=sandbox
